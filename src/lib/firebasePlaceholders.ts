export const firebaseHooks = {
  // Future: initialize Firebase app and services here.
  initialize: () => undefined,

  // Future: sync pages collection with Firestore.
  syncPages: async () => Promise.resolve(),

  // Future: upload media assets to Firebase Storage.
  uploadMedia: async () => Promise.resolve(),

  // Future: generate dynamic links and track scans.
  trackScan: async () => Promise.resolve(),
}
